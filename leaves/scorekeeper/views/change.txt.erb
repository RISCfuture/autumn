<% if var(:unauthorized) -%>
	You can't change <%= var :receiver %>'s points.
<% else -%>
	<%= var(:giver).name %> <%= delta > 0 ? 'gave' : 'docked' %> <%= var(:receiver).name %> <%= var :delta %> <%= var(:delta).pluralize('point') %>
<% end -%>
